services:
  - type: web
    name: jarvis-neo-relay
    runtime: docker
    dockerfilePath: ./remote_relay/Dockerfile
    dockerContext: ./remote_relay
    healthCheckPath: /healthz
    autoDeploy: true
